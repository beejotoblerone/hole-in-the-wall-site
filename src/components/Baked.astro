
<h2>Can I get a Potato...?</h2>

<div class="potato-layout">
    <div class="potato-col">
        <div class="menu-row">
            <span class="menuitem potato">Slutty</span>
            <span class="menu-desc">garlic rosemary butter, melted gruyere, bacon bits</span>
        </div>
        <div class="menu-row">
            <span class="menuitem potato">Foolish</span>
            <span class="menu-desc">mac and cheese, gravy, caramelized onions, fried onions, scallions</span>
        </div>
    </div>

    <div class="potato-image">
        <img src="/src/assets/images/tato.png" alt="potato" />
    </div>

    <div class="potato-col">
        <div class="menu-row">
            <span class="menuitem potato">Drunk</span>
            <span class="menu-desc">beer cheese and caramelized onions</span>
        </div>
        <div class="menu-row">
            <span class="menuitem potato">Evil</span>
            <span class="menu-desc">Melted pepper jack cheese, pulled pork, hot habanero honey and crispy chili oil</span>
        </div>
    </div>
</div>
<style>
    .potato {
        /* use color (not font-color) and a CSS variable with a fallback */
        color: var(--color-darkgreen, darkgreen);
    }

        .potato-layout {
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            gap: 1rem;
            align-items: center;
            width: min(1000px, 95vw);
            margin: 1rem auto;
        }
        .potato-image img { max-width: 280px; width: 100%; height: auto; display:block; margin:0 auto; }
        .potato-col { display: flex; flex-direction: column; gap: 0.5rem; }
    .menu-row { display:flex; flex-direction: column; gap:0.25rem; align-items:flex-start; max-width: 320px; }
    .menuitem { font-weight: 700; display: block; }
    .menu-desc { color: var(--color-text-muted, #666); }

    /* Left column options align toward the image (right); right column align toward image (left) */
    .potato-layout .potato-col:first-child { align-items: flex-end; }
    .potato-layout .potato-col:last-child { align-items: flex-start; }
    .potato-layout .potato-col:first-child .menu-row { align-items: flex-end; text-align: right; }
    .potato-layout .potato-col:last-child .menu-row { align-items: flex-start; text-align: left; }

        @media (max-width: 800px) {
            .potato-layout { grid-template-columns: 1fr; }
            .potato-image { order: 1; }
            .potato-col { order: 2; }
        }
</style>
